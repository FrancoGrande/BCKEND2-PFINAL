export default class OrderDTO{
    constructor(order){
        constructor(orderToValidate){
            this.name= userToValidate.name;
            this.email = userToValidate.email;
            this.role = userToValidate ? userToValidate.role = "user_role";
    }
    }
}